import{NextRequest,NextResponse}from'next/server';import{backendBaseUrl}from'@/lib/backend-proxy';export async function GET(req:NextRequest){const url=new URL('/v1/public/patches',backendBaseUrl());req.nextUrl.searchParams.forEach((v,k)=>url.searchParams.append(k,v));try{const upstream=await fetch(url,{cache:'no-store'});return new NextResponse(await upstream.arrayBuffer(),{status:upstream.status,headers:{'content-type':upstream.headers.get('content-type')||'application/json','cache-control':'public, s-maxage=60'}})}catch{return NextResponse.json({error:'Patch service unavailable'},{status:503})}}
