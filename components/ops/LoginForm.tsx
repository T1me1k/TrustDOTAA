'use client';
import { useState } from 'react';
export default function LoginForm(){ const [error,setError]=useState(''); async function submit(e:React.FormEvent<HTMLFormElement>){ e.preventDefault(); const fd=new FormData(e.currentTarget); const res=await fetch('/api/admin/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:fd.get('email'),password:fd.get('password')})}); if(res.ok) location.href='/ops'; else setError((await res.json()).error||'Login failed'); } return <main className="min-h-screen bg-trust-black bg-radial-trust p-6 text-white"><form onSubmit={submit} className="mx-auto mt-24 max-w-md rounded-[2rem] border border-white/10 bg-white/5 p-8 shadow-glow"><p className="text-xs uppercase tracking-[.35em] text-trust-soft">DEMO ADMIN</p><h1 className="mt-3 text-4xl font-black">TRUST Ops</h1><p className="mt-2 text-sm text-zinc-400">Server session login. Defaults are demo-only unless env secrets are configured.</p><label className="mt-6 block text-sm">Email<input name="email" type="email" required className="mt-2 w-full rounded-2xl border border-white/10 bg-black/40 p-3" defaultValue="admin@trust.local" /></label><label className="mt-4 block text-sm">Password<input name="password" type="password" required className="mt-2 w-full rounded-2xl border border-white/10 bg-black/40 p-3" /></label>{error&&<p className="mt-3 text-rose-300">{error}</p>}<button className="mt-6 w-full rounded-2xl bg-trust-violet py-3 font-black shadow-glow">Sign in</button></form></main> }
