'use client';
import { useState } from 'react';
export default function LoginForm({emailPlaceholder}:{emailPlaceholder:string}){ const [error,setError]=useState(''); async function submit(e:React.FormEvent<HTMLFormElement>){ e.preventDefault(); setError(''); const fd=new FormData(e.currentTarget); try { const res=await fetch('/api/admin/auth/login',{method:'POST',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:fd.get('email'),password:fd.get('password')})}); const data=await res.json().catch(()=>({})); if(res.ok && data.ok===true) location.href='/ops'; else setError(data.error || (res.status===401 ? 'Unauthorized' : 'Login failed')); } catch { setError('Login failed'); } } return <main className="min-h-screen bg-trust-black bg-radial-trust p-6 text-white"><form onSubmit={submit} className="mx-auto mt-24 max-w-md rounded-[2rem] border border-white/10 bg-white/5 p-8 shadow-glow"><p className="text-xs uppercase tracking-[.35em] text-trust-soft">ADMIN</p><h1 className="mt-3 text-4xl font-black">TRUST Ops</h1><p className="mt-2 text-sm text-zinc-400">Sign in with the administrator credentials configured on the server.</p><label className="mt-6 block text-sm">Email<input name="email" type="email" required autoComplete="username" placeholder={emailPlaceholder} className="mt-2 w-full rounded-2xl border border-white/10 bg-black/40 p-3" /></label><label className="mt-4 block text-sm">Password<input name="password" type="password" required autoComplete="current-password" className="mt-2 w-full rounded-2xl border border-white/10 bg-black/40 p-3" /></label>{error&&<p role="alert" className="mt-3 text-rose-300">{error}</p>}<button className="mt-6 w-full rounded-2xl bg-trust-violet py-3 font-black shadow-glow">Sign in</button></form></main> }
